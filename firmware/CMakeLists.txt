# Bullerby Chat Firmware
# ESP-IDF project for ESP32-S3 voice intercom

cmake_minimum_required(VERSION 3.16)

set(EXTRA_COMPONENT_DIRS "")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(bullerby-chat)
